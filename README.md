# 🏨 SmartStay - ระบบจองที่พักและโรงแรมออนไลน์

**SmartStay** คือเว็บแอปพลิเคชันสำหรับจองที่พักแบบครบวงจร พัฒนาด้วยเทคโนโลยี Modern Web ล่าสุด เน้นความรวดเร็ว ใช้งานง่าย และรองรับการทำงานทั้งฝั่งลูกค้าและผู้ดูแลระบบ

---

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)
โปรเจกต์นี้พัฒนาโดยใช้เครื่องมือระดับโลกปี 2025:
* **Next.js 15 (App Router):** เฟรมเวิร์กหลักในการสร้างหน้าเว็บ (Frontend & Backend)
* **Supabase:** ฐานข้อมูล Cloud (PostgreSQL) และระบบยืนยันตัวตน (Authentication)
* **Tailwind CSS:** ตกแต่งหน้าตาเว็บไซต์ (UI Design)
* **Bun:** Runtime ตัวแรง (เร็วกว่า Node.js) สำหรับรันโปรเจกต์
* **Vercel:** สำหรับการ Deploy ขึ้นออนไลน์

---

## 🎮 ฟีเจอร์หลัก (Key Features)

### 👤 สำหรับลูกค้าทั่วไป (User)
* **🏠 Home:** ค้นหาและดูรายการโรงแรมแนะนำ พร้อมราคาเริ่มต้น
* **📝 Details:** ดูรายละเอียดโรงแรม รูปภาพ สิ่งอำนวยความสะดวก และเลือกห้องพัก
* **🛒 Booking:** ระบบจองห้องพัก คำนวณราคาตามวันที่ Check-in/Check-out อัตโนมัติ
* **🔐 Auth:** สมัครสมาชิกและเข้าสู่ระบบ (รองรับ Email/Password)
* **📂 My Bookings:** ดูประวัติการจองของตัวเอง และสถานะการชำระเงิน

### 👑 สำหรับผู้ดูแลระบบ (Admin)
*(เข้าใช้งานด้วยอีเมล: `admin@test.com`)*
* **📊 Dashboard:** ดูภาพรวมรายการจองทั้งหมดจากลูกค้าทุกคน
* **🛠️ Room Management:**
    * เพิ่มห้องพักใหม่ (Upload รูป, ใส่ราคา, สิ่งอำนวยความสะดวก)
    * ลบห้องพักออกจากระบบ

---

## 🚀 วิธีติดตั้งและรันโปรเจกต์ (Getting Started)

### 1. โคลนโปรเจกต์ (Clone)
```bash
git clone [https://github.com/](https://github.com/)[ชื่อ-github-ของคุณ]/smartstay-web.git
cd smartstay-web
````

### 2\. ติดตั้งแพ็กเกจ (Install Dependencies)

เราใช้ **Bun** ในการติดตั้ง (ถ้าไม่มีให้ลง Bun ก่อน หรือใช้ npm แทนได้)

```bash
bun install
```

### 3\. ตั้งค่า Environment Variables

สร้างไฟล์ `.env.local` ที่โฟลเดอร์หลัก แล้วใส่ค่าจาก Supabase Project ของคุณ:

```env
NEXT_PUBLIC_SUPABASE_URL=[ใส่_Project_URL_ของคุณ]
NEXT_PUBLIC_SUPABASE_ANON_KEY=[ใส่_Anon_Key_ของคุณ]
```

### 4\. ตั้งค่าฐานข้อมูล (Supabase Setup)

ไปที่ Supabase SQL Editor แล้วรันคำสั่งสร้างตาราง (Tables):

  * `profiles` (เก็บข้อมูล user)
  * `hotels` (เก็บข้อมูลโรงแรม)
  * `rooms` (เก็บข้อมูลห้องพัก)
  * `bookings` (เก็บข้อมูลการจอง)

> **ข้อควรระวัง:** อย่าลืมปิด **Confirm Email** ในเมนู *Authentication -\> Providers -\> Email*

### 5\. รันโปรเจกต์ (Run)

```bash
bun dev
```

เปิดบราวเซอร์ไปที่: [http://localhost:3000](https://www.google.com/search?q=http://localhost:3000)

-----

## ☁️ วิธีนำขึ้นออนไลน์ (Deployment)

โปรเจกต์นี้รองรับการ Deploy ผ่าน **Vercel** ได้ทันที:

1.  อัปโหลดโค้ดขึ้น **GitHub**
2.  ไปที่ [Vercel.com](https://vercel.com) กด **Import Project**
3.  เลือก Repository นี้
4.  ใส่ **Environment Variables** (ค่าเดียวกับใน `.env.local`)
5.  กด **Deploy** 🚀

-----

## 🧪 ข้อมูลสำหรับทดสอบ (Demo Credentials)

  * **Admin Login:**
      * Email: `admin@test.com`
      * Password: [รหัสผ่านที่คุณตั้ง]
  * **User Login:**
      * สมัครสมาชิกใหม่ได้เลย (ใช้เมลจำลองได้)


```
```